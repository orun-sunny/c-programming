#include <stdio.h>
int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    long long int a,b,c,d,i,e,n=100;
    for (i=1;i<n;i++)
    {
        scanf("%lld",&n,&a);


    }
   // scanf("%lld",&n);

    //scanf("%lld",&a);
    scanf("%d %d %d",&b,&c,&d);
    e=b+c+d;
    printf("%d\n",a-e);
    return 0;
}
