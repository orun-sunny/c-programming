#include <stdio.h>
int main()

{

    int i=0,n;
    scanf("%d",&n);

    while(i<=n)
    {
        printf("%d ", i);
        i--;
    }

    //for(i=1;i<=n;i++)
   // {
       // printf("%d ",i);
   // }
    return 0;
}
